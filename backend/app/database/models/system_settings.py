"""
system_settings.py – Placeholder module for future system-level configuration tables.

This module is imported by scripts/init_test_database.py so that
Base.metadata.create_all() includes any tables defined here.
No tables are defined yet; the module exists to satisfy the import
and avoid ModuleNotFoundError in CI / test database initialisation.
"""

# from app.database import Base  # uncomment when adding real models here
